/*
 * This file is generated by gdbus-codegen, do not modify it.
 *
 * The license of this code is the same as for the D-Bus interface description
 * it was derived from. Note that it links to GLib, so must comply with the
 * LGPL linking clauses.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "org.freedesktop.NetworkManager.Connection.Active.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  guint use_gvariant : 1;
  guint emits_changed_signal : 1;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface org.freedesktop.NetworkManager.Connection.Active
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:NMDBusActiveConnection
 * @title: NMDBusActiveConnection
 * @short_description: Generated C code for the org.freedesktop.NetworkManager.Connection.Active D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-freedesktop-NetworkManager-Connection-Active.top_of_page">org.freedesktop.NetworkManager.Connection.Active</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.freedesktop.NetworkManager.Connection.Active ---- */

static const _ExtendedGDBusArgInfo _nmdbus_active_connection_signal_info_state_changed_ARG_state =
{
  {
    -1,
    (gchar *) "state",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _nmdbus_active_connection_signal_info_state_changed_ARG_reason =
{
  {
    -1,
    (gchar *) "reason",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _nmdbus_active_connection_signal_info_state_changed_ARG_pointers[] =
{
  &_nmdbus_active_connection_signal_info_state_changed_ARG_state.parent_struct,
  &_nmdbus_active_connection_signal_info_state_changed_ARG_reason.parent_struct,
  NULL
};

static const _ExtendedGDBusSignalInfo _nmdbus_active_connection_signal_info_state_changed =
{
  {
    -1,
    (gchar *) "StateChanged",
    (GDBusArgInfo **) &_nmdbus_active_connection_signal_info_state_changed_ARG_pointers,
    NULL
  },
  "state-changed"
};

static const GDBusSignalInfo * const _nmdbus_active_connection_signal_info_pointers[] =
{
  &_nmdbus_active_connection_signal_info_state_changed.parent_struct,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nmdbus_active_connection_property_info_connection =
{
  {
    -1,
    (gchar *) "Connection",
    (gchar *) "o",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "connection",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _nmdbus_active_connection_property_info_specific_object =
{
  {
    -1,
    (gchar *) "SpecificObject",
    (gchar *) "o",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "specific-object",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _nmdbus_active_connection_property_info_id =
{
  {
    -1,
    (gchar *) "Id",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "id",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _nmdbus_active_connection_property_info_uuid =
{
  {
    -1,
    (gchar *) "Uuid",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "uuid",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _nmdbus_active_connection_property_info_type_ =
{
  {
    -1,
    (gchar *) "Type",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "type",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _nmdbus_active_connection_property_info_devices =
{
  {
    -1,
    (gchar *) "Devices",
    (gchar *) "ao",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "devices",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _nmdbus_active_connection_property_info_state =
{
  {
    -1,
    (gchar *) "State",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "state",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _nmdbus_active_connection_property_info_state_flags =
{
  {
    -1,
    (gchar *) "StateFlags",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "state-flags",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _nmdbus_active_connection_property_info_default =
{
  {
    -1,
    (gchar *) "Default",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "default",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _nmdbus_active_connection_property_info_ip4_config =
{
  {
    -1,
    (gchar *) "Ip4Config",
    (gchar *) "o",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "ip4-config",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _nmdbus_active_connection_property_info_dhcp4_config =
{
  {
    -1,
    (gchar *) "Dhcp4Config",
    (gchar *) "o",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "dhcp4-config",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _nmdbus_active_connection_property_info_default6 =
{
  {
    -1,
    (gchar *) "Default6",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "default6",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _nmdbus_active_connection_property_info_ip6_config =
{
  {
    -1,
    (gchar *) "Ip6Config",
    (gchar *) "o",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "ip6-config",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _nmdbus_active_connection_property_info_dhcp6_config =
{
  {
    -1,
    (gchar *) "Dhcp6Config",
    (gchar *) "o",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "dhcp6-config",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _nmdbus_active_connection_property_info_vpn =
{
  {
    -1,
    (gchar *) "Vpn",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "vpn",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _nmdbus_active_connection_property_info_master =
{
  {
    -1,
    (gchar *) "Master",
    (gchar *) "o",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "master",
  FALSE,
  TRUE
};

static const GDBusPropertyInfo * const _nmdbus_active_connection_property_info_pointers[] =
{
  &_nmdbus_active_connection_property_info_connection.parent_struct,
  &_nmdbus_active_connection_property_info_specific_object.parent_struct,
  &_nmdbus_active_connection_property_info_id.parent_struct,
  &_nmdbus_active_connection_property_info_uuid.parent_struct,
  &_nmdbus_active_connection_property_info_type_.parent_struct,
  &_nmdbus_active_connection_property_info_devices.parent_struct,
  &_nmdbus_active_connection_property_info_state.parent_struct,
  &_nmdbus_active_connection_property_info_state_flags.parent_struct,
  &_nmdbus_active_connection_property_info_default.parent_struct,
  &_nmdbus_active_connection_property_info_ip4_config.parent_struct,
  &_nmdbus_active_connection_property_info_dhcp4_config.parent_struct,
  &_nmdbus_active_connection_property_info_default6.parent_struct,
  &_nmdbus_active_connection_property_info_ip6_config.parent_struct,
  &_nmdbus_active_connection_property_info_dhcp6_config.parent_struct,
  &_nmdbus_active_connection_property_info_vpn.parent_struct,
  &_nmdbus_active_connection_property_info_master.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _nmdbus_active_connection_interface_info =
{
  {
    -1,
    (gchar *) "org.freedesktop.NetworkManager.Connection.Active",
    NULL,
    (GDBusSignalInfo **) &_nmdbus_active_connection_signal_info_pointers,
    (GDBusPropertyInfo **) &_nmdbus_active_connection_property_info_pointers,
    NULL
  },
  "active-connection",
};


/**
 * nmdbus_active_connection_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-freedesktop-NetworkManager-Connection-Active.top_of_page">org.freedesktop.NetworkManager.Connection.Active</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
nmdbus_active_connection_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_nmdbus_active_connection_interface_info.parent_struct;
}

/**
 * nmdbus_active_connection_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #NMDBusActiveConnection interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
nmdbus_active_connection_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "connection");
  g_object_class_override_property (klass, property_id_begin++, "specific-object");
  g_object_class_override_property (klass, property_id_begin++, "id");
  g_object_class_override_property (klass, property_id_begin++, "uuid");
  g_object_class_override_property (klass, property_id_begin++, "type");
  g_object_class_override_property (klass, property_id_begin++, "devices");
  g_object_class_override_property (klass, property_id_begin++, "state");
  g_object_class_override_property (klass, property_id_begin++, "state-flags");
  g_object_class_override_property (klass, property_id_begin++, "default");
  g_object_class_override_property (klass, property_id_begin++, "ip4-config");
  g_object_class_override_property (klass, property_id_begin++, "dhcp4-config");
  g_object_class_override_property (klass, property_id_begin++, "default6");
  g_object_class_override_property (klass, property_id_begin++, "ip6-config");
  g_object_class_override_property (klass, property_id_begin++, "dhcp6-config");
  g_object_class_override_property (klass, property_id_begin++, "vpn");
  g_object_class_override_property (klass, property_id_begin++, "master");
  return property_id_begin - 1;
}



/**
 * NMDBusActiveConnection:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-NetworkManager-Connection-Active.top_of_page">org.freedesktop.NetworkManager.Connection.Active</link>.
 */

/**
 * NMDBusActiveConnectionIface:
 * @parent_iface: The parent interface.
 * @get_connection: Getter for the #NMDBusActiveConnection:connection property.
 * @get_default: Getter for the #NMDBusActiveConnection:default property.
 * @get_default6: Getter for the #NMDBusActiveConnection:default6 property.
 * @get_devices: Getter for the #NMDBusActiveConnection:devices property.
 * @get_dhcp4_config: Getter for the #NMDBusActiveConnection:dhcp4-config property.
 * @get_dhcp6_config: Getter for the #NMDBusActiveConnection:dhcp6-config property.
 * @get_id: Getter for the #NMDBusActiveConnection:id property.
 * @get_ip4_config: Getter for the #NMDBusActiveConnection:ip4-config property.
 * @get_ip6_config: Getter for the #NMDBusActiveConnection:ip6-config property.
 * @get_master: Getter for the #NMDBusActiveConnection:master property.
 * @get_specific_object: Getter for the #NMDBusActiveConnection:specific-object property.
 * @get_state: Getter for the #NMDBusActiveConnection:state property.
 * @get_state_flags: Getter for the #NMDBusActiveConnection:state-flags property.
 * @get_type_: Getter for the #NMDBusActiveConnection:type property.
 * @get_uuid: Getter for the #NMDBusActiveConnection:uuid property.
 * @get_vpn: Getter for the #NMDBusActiveConnection:vpn property.
 * @state_changed: Handler for the #NMDBusActiveConnection::state-changed signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-NetworkManager-Connection-Active.top_of_page">org.freedesktop.NetworkManager.Connection.Active</link>.
 */

typedef NMDBusActiveConnectionIface NMDBusActiveConnectionInterface;
G_DEFINE_INTERFACE (NMDBusActiveConnection, nmdbus_active_connection, G_TYPE_OBJECT)

static void
nmdbus_active_connection_default_init (NMDBusActiveConnectionIface *iface)
{
  /* GObject signals for received D-Bus signals: */
  /**
   * NMDBusActiveConnection::state-changed:
   * @object: A #NMDBusActiveConnection.
   * @arg_state: Argument.
   * @arg_reason: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-freedesktop-NetworkManager-Connection-Active.StateChanged">"StateChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("state-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NMDBusActiveConnectionIface, state_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_UINT, G_TYPE_UINT);

  /* GObject properties for D-Bus properties: */
  /**
   * NMDBusActiveConnection:connection:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Connection">"Connection"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("connection", "Connection", "Connection", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NMDBusActiveConnection:specific-object:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.SpecificObject">"SpecificObject"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("specific-object", "SpecificObject", "SpecificObject", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NMDBusActiveConnection:id:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Id">"Id"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("id", "Id", "Id", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NMDBusActiveConnection:uuid:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Uuid">"Uuid"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("uuid", "Uuid", "Uuid", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NMDBusActiveConnection:type:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Type">"Type"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("type", "Type", "Type", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NMDBusActiveConnection:devices:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Devices">"Devices"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("devices", "Devices", "Devices", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NMDBusActiveConnection:state:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.State">"State"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("state", "State", "State", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NMDBusActiveConnection:state-flags:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.StateFlags">"StateFlags"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("state-flags", "StateFlags", "StateFlags", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NMDBusActiveConnection:default:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Default">"Default"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("default", "Default", "Default", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NMDBusActiveConnection:ip4-config:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Ip4Config">"Ip4Config"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("ip4-config", "Ip4Config", "Ip4Config", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NMDBusActiveConnection:dhcp4-config:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Dhcp4Config">"Dhcp4Config"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("dhcp4-config", "Dhcp4Config", "Dhcp4Config", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NMDBusActiveConnection:default6:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Default6">"Default6"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("default6", "Default6", "Default6", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NMDBusActiveConnection:ip6-config:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Ip6Config">"Ip6Config"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("ip6-config", "Ip6Config", "Ip6Config", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NMDBusActiveConnection:dhcp6-config:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Dhcp6Config">"Dhcp6Config"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("dhcp6-config", "Dhcp6Config", "Dhcp6Config", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NMDBusActiveConnection:vpn:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Vpn">"Vpn"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("vpn", "Vpn", "Vpn", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NMDBusActiveConnection:master:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Master">"Master"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("master", "Master", "Master", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * nmdbus_active_connection_get_connection: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Connection">"Connection"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nmdbus_active_connection_dup_connection() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
nmdbus_active_connection_get_connection (NMDBusActiveConnection *object)
{
  return NMDBUS_ACTIVE_CONNECTION_GET_IFACE (object)->get_connection (object);
}

/**
 * nmdbus_active_connection_dup_connection: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Connection">"Connection"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
nmdbus_active_connection_dup_connection (NMDBusActiveConnection *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "connection", &value, NULL);
  return value;
}

/**
 * nmdbus_active_connection_set_connection: (skip)
 * @object: A #NMDBusActiveConnection.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Connection">"Connection"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_active_connection_set_connection (NMDBusActiveConnection *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "connection", value, NULL);
}

/**
 * nmdbus_active_connection_get_specific_object: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.SpecificObject">"SpecificObject"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nmdbus_active_connection_dup_specific_object() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
nmdbus_active_connection_get_specific_object (NMDBusActiveConnection *object)
{
  return NMDBUS_ACTIVE_CONNECTION_GET_IFACE (object)->get_specific_object (object);
}

/**
 * nmdbus_active_connection_dup_specific_object: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.SpecificObject">"SpecificObject"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
nmdbus_active_connection_dup_specific_object (NMDBusActiveConnection *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "specific-object", &value, NULL);
  return value;
}

/**
 * nmdbus_active_connection_set_specific_object: (skip)
 * @object: A #NMDBusActiveConnection.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.SpecificObject">"SpecificObject"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_active_connection_set_specific_object (NMDBusActiveConnection *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "specific-object", value, NULL);
}

/**
 * nmdbus_active_connection_get_id: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Id">"Id"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nmdbus_active_connection_dup_id() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
nmdbus_active_connection_get_id (NMDBusActiveConnection *object)
{
  return NMDBUS_ACTIVE_CONNECTION_GET_IFACE (object)->get_id (object);
}

/**
 * nmdbus_active_connection_dup_id: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Id">"Id"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
nmdbus_active_connection_dup_id (NMDBusActiveConnection *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "id", &value, NULL);
  return value;
}

/**
 * nmdbus_active_connection_set_id: (skip)
 * @object: A #NMDBusActiveConnection.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Id">"Id"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_active_connection_set_id (NMDBusActiveConnection *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "id", value, NULL);
}

/**
 * nmdbus_active_connection_get_uuid: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Uuid">"Uuid"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nmdbus_active_connection_dup_uuid() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
nmdbus_active_connection_get_uuid (NMDBusActiveConnection *object)
{
  return NMDBUS_ACTIVE_CONNECTION_GET_IFACE (object)->get_uuid (object);
}

/**
 * nmdbus_active_connection_dup_uuid: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Uuid">"Uuid"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
nmdbus_active_connection_dup_uuid (NMDBusActiveConnection *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "uuid", &value, NULL);
  return value;
}

/**
 * nmdbus_active_connection_set_uuid: (skip)
 * @object: A #NMDBusActiveConnection.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Uuid">"Uuid"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_active_connection_set_uuid (NMDBusActiveConnection *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "uuid", value, NULL);
}

/**
 * nmdbus_active_connection_get_type_: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Type">"Type"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nmdbus_active_connection_dup_type_() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
nmdbus_active_connection_get_type_ (NMDBusActiveConnection *object)
{
  return NMDBUS_ACTIVE_CONNECTION_GET_IFACE (object)->get_type_ (object);
}

/**
 * nmdbus_active_connection_dup_type_: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Type">"Type"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
nmdbus_active_connection_dup_type_ (NMDBusActiveConnection *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "type", &value, NULL);
  return value;
}

/**
 * nmdbus_active_connection_set_type_: (skip)
 * @object: A #NMDBusActiveConnection.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Type">"Type"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_active_connection_set_type_ (NMDBusActiveConnection *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "type", value, NULL);
}

/**
 * nmdbus_active_connection_get_devices: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Devices">"Devices"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nmdbus_active_connection_dup_devices() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
nmdbus_active_connection_get_devices (NMDBusActiveConnection *object)
{
  return NMDBUS_ACTIVE_CONNECTION_GET_IFACE (object)->get_devices (object);
}

/**
 * nmdbus_active_connection_dup_devices: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Devices">"Devices"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
nmdbus_active_connection_dup_devices (NMDBusActiveConnection *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "devices", &value, NULL);
  return value;
}

/**
 * nmdbus_active_connection_set_devices: (skip)
 * @object: A #NMDBusActiveConnection.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Devices">"Devices"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_active_connection_set_devices (NMDBusActiveConnection *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "devices", value, NULL);
}

/**
 * nmdbus_active_connection_get_state: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.State">"State"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
nmdbus_active_connection_get_state (NMDBusActiveConnection *object)
{
  return NMDBUS_ACTIVE_CONNECTION_GET_IFACE (object)->get_state (object);
}

/**
 * nmdbus_active_connection_set_state: (skip)
 * @object: A #NMDBusActiveConnection.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.State">"State"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_active_connection_set_state (NMDBusActiveConnection *object, guint value)
{
  g_object_set (G_OBJECT (object), "state", value, NULL);
}

/**
 * nmdbus_active_connection_get_state_flags: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.StateFlags">"StateFlags"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
nmdbus_active_connection_get_state_flags (NMDBusActiveConnection *object)
{
  return NMDBUS_ACTIVE_CONNECTION_GET_IFACE (object)->get_state_flags (object);
}

/**
 * nmdbus_active_connection_set_state_flags: (skip)
 * @object: A #NMDBusActiveConnection.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.StateFlags">"StateFlags"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_active_connection_set_state_flags (NMDBusActiveConnection *object, guint value)
{
  g_object_set (G_OBJECT (object), "state-flags", value, NULL);
}

/**
 * nmdbus_active_connection_get_default: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Default">"Default"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
nmdbus_active_connection_get_default (NMDBusActiveConnection *object)
{
  return NMDBUS_ACTIVE_CONNECTION_GET_IFACE (object)->get_default (object);
}

/**
 * nmdbus_active_connection_set_default: (skip)
 * @object: A #NMDBusActiveConnection.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Default">"Default"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_active_connection_set_default (NMDBusActiveConnection *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "default", value, NULL);
}

/**
 * nmdbus_active_connection_get_ip4_config: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Ip4Config">"Ip4Config"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nmdbus_active_connection_dup_ip4_config() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
nmdbus_active_connection_get_ip4_config (NMDBusActiveConnection *object)
{
  return NMDBUS_ACTIVE_CONNECTION_GET_IFACE (object)->get_ip4_config (object);
}

/**
 * nmdbus_active_connection_dup_ip4_config: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Ip4Config">"Ip4Config"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
nmdbus_active_connection_dup_ip4_config (NMDBusActiveConnection *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "ip4-config", &value, NULL);
  return value;
}

/**
 * nmdbus_active_connection_set_ip4_config: (skip)
 * @object: A #NMDBusActiveConnection.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Ip4Config">"Ip4Config"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_active_connection_set_ip4_config (NMDBusActiveConnection *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "ip4-config", value, NULL);
}

/**
 * nmdbus_active_connection_get_dhcp4_config: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Dhcp4Config">"Dhcp4Config"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nmdbus_active_connection_dup_dhcp4_config() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
nmdbus_active_connection_get_dhcp4_config (NMDBusActiveConnection *object)
{
  return NMDBUS_ACTIVE_CONNECTION_GET_IFACE (object)->get_dhcp4_config (object);
}

/**
 * nmdbus_active_connection_dup_dhcp4_config: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Dhcp4Config">"Dhcp4Config"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
nmdbus_active_connection_dup_dhcp4_config (NMDBusActiveConnection *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "dhcp4-config", &value, NULL);
  return value;
}

/**
 * nmdbus_active_connection_set_dhcp4_config: (skip)
 * @object: A #NMDBusActiveConnection.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Dhcp4Config">"Dhcp4Config"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_active_connection_set_dhcp4_config (NMDBusActiveConnection *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "dhcp4-config", value, NULL);
}

/**
 * nmdbus_active_connection_get_default6: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Default6">"Default6"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
nmdbus_active_connection_get_default6 (NMDBusActiveConnection *object)
{
  return NMDBUS_ACTIVE_CONNECTION_GET_IFACE (object)->get_default6 (object);
}

/**
 * nmdbus_active_connection_set_default6: (skip)
 * @object: A #NMDBusActiveConnection.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Default6">"Default6"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_active_connection_set_default6 (NMDBusActiveConnection *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "default6", value, NULL);
}

/**
 * nmdbus_active_connection_get_ip6_config: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Ip6Config">"Ip6Config"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nmdbus_active_connection_dup_ip6_config() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
nmdbus_active_connection_get_ip6_config (NMDBusActiveConnection *object)
{
  return NMDBUS_ACTIVE_CONNECTION_GET_IFACE (object)->get_ip6_config (object);
}

/**
 * nmdbus_active_connection_dup_ip6_config: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Ip6Config">"Ip6Config"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
nmdbus_active_connection_dup_ip6_config (NMDBusActiveConnection *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "ip6-config", &value, NULL);
  return value;
}

/**
 * nmdbus_active_connection_set_ip6_config: (skip)
 * @object: A #NMDBusActiveConnection.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Ip6Config">"Ip6Config"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_active_connection_set_ip6_config (NMDBusActiveConnection *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "ip6-config", value, NULL);
}

/**
 * nmdbus_active_connection_get_dhcp6_config: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Dhcp6Config">"Dhcp6Config"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nmdbus_active_connection_dup_dhcp6_config() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
nmdbus_active_connection_get_dhcp6_config (NMDBusActiveConnection *object)
{
  return NMDBUS_ACTIVE_CONNECTION_GET_IFACE (object)->get_dhcp6_config (object);
}

/**
 * nmdbus_active_connection_dup_dhcp6_config: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Dhcp6Config">"Dhcp6Config"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
nmdbus_active_connection_dup_dhcp6_config (NMDBusActiveConnection *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "dhcp6-config", &value, NULL);
  return value;
}

/**
 * nmdbus_active_connection_set_dhcp6_config: (skip)
 * @object: A #NMDBusActiveConnection.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Dhcp6Config">"Dhcp6Config"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_active_connection_set_dhcp6_config (NMDBusActiveConnection *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "dhcp6-config", value, NULL);
}

/**
 * nmdbus_active_connection_get_vpn: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Vpn">"Vpn"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
nmdbus_active_connection_get_vpn (NMDBusActiveConnection *object)
{
  return NMDBUS_ACTIVE_CONNECTION_GET_IFACE (object)->get_vpn (object);
}

/**
 * nmdbus_active_connection_set_vpn: (skip)
 * @object: A #NMDBusActiveConnection.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Vpn">"Vpn"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_active_connection_set_vpn (NMDBusActiveConnection *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "vpn", value, NULL);
}

/**
 * nmdbus_active_connection_get_master: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Master">"Master"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nmdbus_active_connection_dup_master() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
nmdbus_active_connection_get_master (NMDBusActiveConnection *object)
{
  return NMDBUS_ACTIVE_CONNECTION_GET_IFACE (object)->get_master (object);
}

/**
 * nmdbus_active_connection_dup_master: (skip)
 * @object: A #NMDBusActiveConnection.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Master">"Master"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
nmdbus_active_connection_dup_master (NMDBusActiveConnection *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "master", &value, NULL);
  return value;
}

/**
 * nmdbus_active_connection_set_master: (skip)
 * @object: A #NMDBusActiveConnection.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Connection-Active.Master">"Master"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_active_connection_set_master (NMDBusActiveConnection *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "master", value, NULL);
}

/**
 * nmdbus_active_connection_emit_state_changed:
 * @object: A #NMDBusActiveConnection.
 * @arg_state: Argument to pass with the signal.
 * @arg_reason: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-freedesktop-NetworkManager-Connection-Active.StateChanged">"StateChanged"</link> D-Bus signal.
 */
void
nmdbus_active_connection_emit_state_changed (
    NMDBusActiveConnection *object,
    guint arg_state,
    guint arg_reason)
{
  g_signal_emit_by_name (object, "state-changed", arg_state, arg_reason);
}

/* ------------------------------------------------------------------------ */

/**
 * NMDBusActiveConnectionProxy:
 *
 * The #NMDBusActiveConnectionProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * NMDBusActiveConnectionProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #NMDBusActiveConnectionProxy.
 */

struct _NMDBusActiveConnectionProxyPrivate
{
  GData *qdata;
};

static void nmdbus_active_connection_proxy_iface_init (NMDBusActiveConnectionIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (NMDBusActiveConnectionProxy, nmdbus_active_connection_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (NMDBusActiveConnectionProxy)
                         G_IMPLEMENT_INTERFACE (NMDBUS_TYPE_ACTIVE_CONNECTION, nmdbus_active_connection_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (NMDBusActiveConnectionProxy, nmdbus_active_connection_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (NMDBUS_TYPE_ACTIVE_CONNECTION, nmdbus_active_connection_proxy_iface_init))

#endif
static void
nmdbus_active_connection_proxy_finalize (GObject *object)
{
  NMDBusActiveConnectionProxy *proxy = NMDBUS_ACTIVE_CONNECTION_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (nmdbus_active_connection_proxy_parent_class)->finalize (object);
}

static void
nmdbus_active_connection_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 16);
  info = (const _ExtendedGDBusPropertyInfo *) _nmdbus_active_connection_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
nmdbus_active_connection_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface org.freedesktop.NetworkManager.Connection.Active: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
nmdbus_active_connection_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 16);
  info = (const _ExtendedGDBusPropertyInfo *) _nmdbus_active_connection_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.freedesktop.NetworkManager.Connection.Active", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) nmdbus_active_connection_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
nmdbus_active_connection_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_nmdbus_active_connection_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], NMDBUS_TYPE_ACTIVE_CONNECTION);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, NMDBUS_TYPE_ACTIVE_CONNECTION);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
nmdbus_active_connection_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  NMDBusActiveConnectionProxy *proxy = NMDBUS_ACTIVE_CONNECTION_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nmdbus_active_connection_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nmdbus_active_connection_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *
nmdbus_active_connection_proxy_get_connection (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionProxy *proxy = NMDBUS_ACTIVE_CONNECTION_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Connection");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
nmdbus_active_connection_proxy_get_specific_object (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionProxy *proxy = NMDBUS_ACTIVE_CONNECTION_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "SpecificObject");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
nmdbus_active_connection_proxy_get_id (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionProxy *proxy = NMDBUS_ACTIVE_CONNECTION_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Id");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
nmdbus_active_connection_proxy_get_uuid (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionProxy *proxy = NMDBUS_ACTIVE_CONNECTION_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Uuid");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
nmdbus_active_connection_proxy_get_type_ (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionProxy *proxy = NMDBUS_ACTIVE_CONNECTION_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Type");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *const *
nmdbus_active_connection_proxy_get_devices (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionProxy *proxy = NMDBUS_ACTIVE_CONNECTION_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  value = g_datalist_get_data (&proxy->priv->qdata, "Devices");
  if (value != NULL)
    return value;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Devices");
  if (variant != NULL)
    {
      value = g_variant_get_objv (variant, NULL);
      g_datalist_set_data_full (&proxy->priv->qdata, "Devices", (gpointer) value, g_free);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
nmdbus_active_connection_proxy_get_state (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionProxy *proxy = NMDBUS_ACTIVE_CONNECTION_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "State");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
nmdbus_active_connection_proxy_get_state_flags (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionProxy *proxy = NMDBUS_ACTIVE_CONNECTION_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "StateFlags");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
nmdbus_active_connection_proxy_get_default (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionProxy *proxy = NMDBUS_ACTIVE_CONNECTION_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Default");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
nmdbus_active_connection_proxy_get_ip4_config (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionProxy *proxy = NMDBUS_ACTIVE_CONNECTION_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Ip4Config");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
nmdbus_active_connection_proxy_get_dhcp4_config (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionProxy *proxy = NMDBUS_ACTIVE_CONNECTION_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Dhcp4Config");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
nmdbus_active_connection_proxy_get_default6 (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionProxy *proxy = NMDBUS_ACTIVE_CONNECTION_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Default6");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
nmdbus_active_connection_proxy_get_ip6_config (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionProxy *proxy = NMDBUS_ACTIVE_CONNECTION_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Ip6Config");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
nmdbus_active_connection_proxy_get_dhcp6_config (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionProxy *proxy = NMDBUS_ACTIVE_CONNECTION_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Dhcp6Config");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
nmdbus_active_connection_proxy_get_vpn (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionProxy *proxy = NMDBUS_ACTIVE_CONNECTION_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Vpn");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
nmdbus_active_connection_proxy_get_master (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionProxy *proxy = NMDBUS_ACTIVE_CONNECTION_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Master");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static void
nmdbus_active_connection_proxy_init (NMDBusActiveConnectionProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = nmdbus_active_connection_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, NMDBUS_TYPE_ACTIVE_CONNECTION_PROXY, NMDBusActiveConnectionProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), nmdbus_active_connection_interface_info ());
}

static void
nmdbus_active_connection_proxy_class_init (NMDBusActiveConnectionProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = nmdbus_active_connection_proxy_finalize;
  gobject_class->get_property = nmdbus_active_connection_proxy_get_property;
  gobject_class->set_property = nmdbus_active_connection_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = nmdbus_active_connection_proxy_g_signal;
  proxy_class->g_properties_changed = nmdbus_active_connection_proxy_g_properties_changed;

  nmdbus_active_connection_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (NMDBusActiveConnectionProxyPrivate));
#endif
}

static void
nmdbus_active_connection_proxy_iface_init (NMDBusActiveConnectionIface *iface)
{
  iface->get_connection = nmdbus_active_connection_proxy_get_connection;
  iface->get_specific_object = nmdbus_active_connection_proxy_get_specific_object;
  iface->get_id = nmdbus_active_connection_proxy_get_id;
  iface->get_uuid = nmdbus_active_connection_proxy_get_uuid;
  iface->get_type_ = nmdbus_active_connection_proxy_get_type_;
  iface->get_devices = nmdbus_active_connection_proxy_get_devices;
  iface->get_state = nmdbus_active_connection_proxy_get_state;
  iface->get_state_flags = nmdbus_active_connection_proxy_get_state_flags;
  iface->get_default = nmdbus_active_connection_proxy_get_default;
  iface->get_ip4_config = nmdbus_active_connection_proxy_get_ip4_config;
  iface->get_dhcp4_config = nmdbus_active_connection_proxy_get_dhcp4_config;
  iface->get_default6 = nmdbus_active_connection_proxy_get_default6;
  iface->get_ip6_config = nmdbus_active_connection_proxy_get_ip6_config;
  iface->get_dhcp6_config = nmdbus_active_connection_proxy_get_dhcp6_config;
  iface->get_vpn = nmdbus_active_connection_proxy_get_vpn;
  iface->get_master = nmdbus_active_connection_proxy_get_master;
}

/**
 * nmdbus_active_connection_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-NetworkManager-Connection-Active.top_of_page">org.freedesktop.NetworkManager.Connection.Active</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call nmdbus_active_connection_proxy_new_finish() to get the result of the operation.
 *
 * See nmdbus_active_connection_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
nmdbus_active_connection_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (NMDBUS_TYPE_ACTIVE_CONNECTION_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.NetworkManager.Connection.Active", NULL);
}

/**
 * nmdbus_active_connection_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nmdbus_active_connection_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with nmdbus_active_connection_proxy_new().
 *
 * Returns: (transfer full) (type NMDBusActiveConnectionProxy): The constructed proxy object or %NULL if @error is set.
 */
NMDBusActiveConnection *
nmdbus_active_connection_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return NMDBUS_ACTIVE_CONNECTION (ret);
  else
    return NULL;
}

/**
 * nmdbus_active_connection_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-NetworkManager-Connection-Active.top_of_page">org.freedesktop.NetworkManager.Connection.Active</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See nmdbus_active_connection_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type NMDBusActiveConnectionProxy): The constructed proxy object or %NULL if @error is set.
 */
NMDBusActiveConnection *
nmdbus_active_connection_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (NMDBUS_TYPE_ACTIVE_CONNECTION_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.NetworkManager.Connection.Active", NULL);
  if (ret != NULL)
    return NMDBUS_ACTIVE_CONNECTION (ret);
  else
    return NULL;
}


/**
 * nmdbus_active_connection_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like nmdbus_active_connection_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call nmdbus_active_connection_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See nmdbus_active_connection_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
nmdbus_active_connection_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (NMDBUS_TYPE_ACTIVE_CONNECTION_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.NetworkManager.Connection.Active", NULL);
}

/**
 * nmdbus_active_connection_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nmdbus_active_connection_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with nmdbus_active_connection_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type NMDBusActiveConnectionProxy): The constructed proxy object or %NULL if @error is set.
 */
NMDBusActiveConnection *
nmdbus_active_connection_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return NMDBUS_ACTIVE_CONNECTION (ret);
  else
    return NULL;
}

/**
 * nmdbus_active_connection_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like nmdbus_active_connection_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See nmdbus_active_connection_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type NMDBusActiveConnectionProxy): The constructed proxy object or %NULL if @error is set.
 */
NMDBusActiveConnection *
nmdbus_active_connection_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (NMDBUS_TYPE_ACTIVE_CONNECTION_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.NetworkManager.Connection.Active", NULL);
  if (ret != NULL)
    return NMDBUS_ACTIVE_CONNECTION (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * NMDBusActiveConnectionSkeleton:
 *
 * The #NMDBusActiveConnectionSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * NMDBusActiveConnectionSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #NMDBusActiveConnectionSkeleton.
 */

struct _NMDBusActiveConnectionSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_nmdbus_active_connection_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], NMDBUS_TYPE_ACTIVE_CONNECTION);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, NMDBUS_TYPE_ACTIVE_CONNECTION);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_nmdbus_active_connection_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nmdbus_active_connection_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_nmdbus_active_connection_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nmdbus_active_connection_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _nmdbus_active_connection_skeleton_vtable =
{
  _nmdbus_active_connection_skeleton_handle_method_call,
  _nmdbus_active_connection_skeleton_handle_get_property,
  _nmdbus_active_connection_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
nmdbus_active_connection_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return nmdbus_active_connection_interface_info ();
}

static GDBusInterfaceVTable *
nmdbus_active_connection_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_nmdbus_active_connection_skeleton_vtable;
}

static GVariant *
nmdbus_active_connection_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_nmdbus_active_connection_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _nmdbus_active_connection_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _nmdbus_active_connection_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _nmdbus_active_connection_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.NetworkManager.Connection.Active", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _nmdbus_active_connection_emit_changed (gpointer user_data);

static void
nmdbus_active_connection_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _nmdbus_active_connection_emit_changed (skeleton);
}

static void
_nmdbus_active_connection_on_signal_state_changed (
    NMDBusActiveConnection *object,
    guint arg_state,
    guint arg_reason)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(uu)",
                   arg_state,
                   arg_reason));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.NetworkManager.Connection.Active", "StateChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void nmdbus_active_connection_skeleton_iface_init (NMDBusActiveConnectionIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (NMDBusActiveConnectionSkeleton, nmdbus_active_connection_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (NMDBusActiveConnectionSkeleton)
                         G_IMPLEMENT_INTERFACE (NMDBUS_TYPE_ACTIVE_CONNECTION, nmdbus_active_connection_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (NMDBusActiveConnectionSkeleton, nmdbus_active_connection_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (NMDBUS_TYPE_ACTIVE_CONNECTION, nmdbus_active_connection_skeleton_iface_init))

#endif
static void
nmdbus_active_connection_skeleton_finalize (GObject *object)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (object);
  guint n;
  for (n = 0; n < 16; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (nmdbus_active_connection_skeleton_parent_class)->finalize (object);
}

static void
nmdbus_active_connection_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 16);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_nmdbus_active_connection_emit_changed (gpointer user_data)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.freedesktop.NetworkManager.Connection.Active",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_nmdbus_active_connection_schedule_emit_changed (NMDBusActiveConnectionSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
nmdbus_active_connection_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _nmdbus_active_connection_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _nmdbus_active_connection_emit_changed");
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
nmdbus_active_connection_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 16);
  info = (const _ExtendedGDBusPropertyInfo *) _nmdbus_active_connection_property_info_pointers[prop_id - 1];
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL &&
          info->emits_changed_signal)
        _nmdbus_active_connection_schedule_emit_changed (skeleton, info, prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
nmdbus_active_connection_skeleton_init (NMDBusActiveConnectionSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = nmdbus_active_connection_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, NMDBUS_TYPE_ACTIVE_CONNECTION_SKELETON, NMDBusActiveConnectionSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 16);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[3], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[4], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[5], G_TYPE_STRV);
  g_value_init (&skeleton->priv->properties[6], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[7], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[8], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[9], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[10], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[11], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[12], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[13], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[14], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[15], G_TYPE_STRING);
}

static const gchar *
nmdbus_active_connection_skeleton_get_connection (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
nmdbus_active_connection_skeleton_get_specific_object (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
nmdbus_active_connection_skeleton_get_id (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
nmdbus_active_connection_skeleton_get_uuid (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[3]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
nmdbus_active_connection_skeleton_get_type_ (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[4]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *const *
nmdbus_active_connection_skeleton_get_devices (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[5]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
nmdbus_active_connection_skeleton_get_state (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[6]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
nmdbus_active_connection_skeleton_get_state_flags (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[7]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
nmdbus_active_connection_skeleton_get_default (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[8]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
nmdbus_active_connection_skeleton_get_ip4_config (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[9]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
nmdbus_active_connection_skeleton_get_dhcp4_config (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[10]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
nmdbus_active_connection_skeleton_get_default6 (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[11]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
nmdbus_active_connection_skeleton_get_ip6_config (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[12]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
nmdbus_active_connection_skeleton_get_dhcp6_config (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[13]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
nmdbus_active_connection_skeleton_get_vpn (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[14]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
nmdbus_active_connection_skeleton_get_master (NMDBusActiveConnection *object)
{
  NMDBusActiveConnectionSkeleton *skeleton = NMDBUS_ACTIVE_CONNECTION_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[15]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
nmdbus_active_connection_skeleton_class_init (NMDBusActiveConnectionSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = nmdbus_active_connection_skeleton_finalize;
  gobject_class->get_property = nmdbus_active_connection_skeleton_get_property;
  gobject_class->set_property = nmdbus_active_connection_skeleton_set_property;
  gobject_class->notify       = nmdbus_active_connection_skeleton_notify;


  nmdbus_active_connection_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = nmdbus_active_connection_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = nmdbus_active_connection_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = nmdbus_active_connection_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = nmdbus_active_connection_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (NMDBusActiveConnectionSkeletonPrivate));
#endif
}

static void
nmdbus_active_connection_skeleton_iface_init (NMDBusActiveConnectionIface *iface)
{
  iface->state_changed = _nmdbus_active_connection_on_signal_state_changed;
  iface->get_connection = nmdbus_active_connection_skeleton_get_connection;
  iface->get_specific_object = nmdbus_active_connection_skeleton_get_specific_object;
  iface->get_id = nmdbus_active_connection_skeleton_get_id;
  iface->get_uuid = nmdbus_active_connection_skeleton_get_uuid;
  iface->get_type_ = nmdbus_active_connection_skeleton_get_type_;
  iface->get_devices = nmdbus_active_connection_skeleton_get_devices;
  iface->get_state = nmdbus_active_connection_skeleton_get_state;
  iface->get_state_flags = nmdbus_active_connection_skeleton_get_state_flags;
  iface->get_default = nmdbus_active_connection_skeleton_get_default;
  iface->get_ip4_config = nmdbus_active_connection_skeleton_get_ip4_config;
  iface->get_dhcp4_config = nmdbus_active_connection_skeleton_get_dhcp4_config;
  iface->get_default6 = nmdbus_active_connection_skeleton_get_default6;
  iface->get_ip6_config = nmdbus_active_connection_skeleton_get_ip6_config;
  iface->get_dhcp6_config = nmdbus_active_connection_skeleton_get_dhcp6_config;
  iface->get_vpn = nmdbus_active_connection_skeleton_get_vpn;
  iface->get_master = nmdbus_active_connection_skeleton_get_master;
}

/**
 * nmdbus_active_connection_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-NetworkManager-Connection-Active.top_of_page">org.freedesktop.NetworkManager.Connection.Active</link>.
 *
 * Returns: (transfer full) (type NMDBusActiveConnectionSkeleton): The skeleton object.
 */
NMDBusActiveConnection *
nmdbus_active_connection_skeleton_new (void)
{
  return NMDBUS_ACTIVE_CONNECTION (g_object_new (NMDBUS_TYPE_ACTIVE_CONNECTION_SKELETON, NULL));
}

