/*
 * This file is generated by gdbus-codegen, do not modify it.
 *
 * The license of this code is the same as for the D-Bus interface description
 * it was derived from. Note that it links to GLib, so must comply with the
 * LGPL linking clauses.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "org.freedesktop.NetworkManager.Device.WiMax.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  guint use_gvariant : 1;
  guint emits_changed_signal : 1;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface org.freedesktop.NetworkManager.Device.WiMax
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:NMDBusDeviceWiMax
 * @title: NMDBusDeviceWiMax
 * @short_description: Generated C code for the org.freedesktop.NetworkManager.Device.WiMax D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-freedesktop-NetworkManager-Device-WiMax.top_of_page">org.freedesktop.NetworkManager.Device.WiMax</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.freedesktop.NetworkManager.Device.WiMax ---- */

static const _ExtendedGDBusArgInfo _nmdbus_device_wi_max_method_info_get_nsp_list_OUT_ARG_nsps =
{
  {
    -1,
    (gchar *) "nsps",
    (gchar *) "ao",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _nmdbus_device_wi_max_method_info_get_nsp_list_OUT_ARG_pointers[] =
{
  &_nmdbus_device_wi_max_method_info_get_nsp_list_OUT_ARG_nsps.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _nmdbus_device_wi_max_method_info_get_nsp_list =
{
  {
    -1,
    (gchar *) "GetNspList",
    NULL,
    (GDBusArgInfo **) &_nmdbus_device_wi_max_method_info_get_nsp_list_OUT_ARG_pointers,
    NULL
  },
  "handle-get-nsp-list",
  FALSE
};

static const GDBusMethodInfo * const _nmdbus_device_wi_max_method_info_pointers[] =
{
  &_nmdbus_device_wi_max_method_info_get_nsp_list.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _nmdbus_device_wi_max_signal_info_nsp_added_ARG_nsp =
{
  {
    -1,
    (gchar *) "nsp",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _nmdbus_device_wi_max_signal_info_nsp_added_ARG_pointers[] =
{
  &_nmdbus_device_wi_max_signal_info_nsp_added_ARG_nsp.parent_struct,
  NULL
};

static const _ExtendedGDBusSignalInfo _nmdbus_device_wi_max_signal_info_nsp_added =
{
  {
    -1,
    (gchar *) "NspAdded",
    (GDBusArgInfo **) &_nmdbus_device_wi_max_signal_info_nsp_added_ARG_pointers,
    NULL
  },
  "nsp-added"
};

static const _ExtendedGDBusArgInfo _nmdbus_device_wi_max_signal_info_nsp_removed_ARG_nsp =
{
  {
    -1,
    (gchar *) "nsp",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _nmdbus_device_wi_max_signal_info_nsp_removed_ARG_pointers[] =
{
  &_nmdbus_device_wi_max_signal_info_nsp_removed_ARG_nsp.parent_struct,
  NULL
};

static const _ExtendedGDBusSignalInfo _nmdbus_device_wi_max_signal_info_nsp_removed =
{
  {
    -1,
    (gchar *) "NspRemoved",
    (GDBusArgInfo **) &_nmdbus_device_wi_max_signal_info_nsp_removed_ARG_pointers,
    NULL
  },
  "nsp-removed"
};

static const GDBusSignalInfo * const _nmdbus_device_wi_max_signal_info_pointers[] =
{
  &_nmdbus_device_wi_max_signal_info_nsp_added.parent_struct,
  &_nmdbus_device_wi_max_signal_info_nsp_removed.parent_struct,
  NULL
};

static const _ExtendedGDBusPropertyInfo _nmdbus_device_wi_max_property_info_nsps =
{
  {
    -1,
    (gchar *) "Nsps",
    (gchar *) "ao",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "nsps",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _nmdbus_device_wi_max_property_info_hw_address =
{
  {
    -1,
    (gchar *) "HwAddress",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "hw-address",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _nmdbus_device_wi_max_property_info_center_frequency =
{
  {
    -1,
    (gchar *) "CenterFrequency",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "center-frequency",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _nmdbus_device_wi_max_property_info_rssi =
{
  {
    -1,
    (gchar *) "Rssi",
    (gchar *) "i",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "rssi",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _nmdbus_device_wi_max_property_info_cinr =
{
  {
    -1,
    (gchar *) "Cinr",
    (gchar *) "i",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "cinr",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _nmdbus_device_wi_max_property_info_tx_power =
{
  {
    -1,
    (gchar *) "TxPower",
    (gchar *) "i",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "tx-power",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _nmdbus_device_wi_max_property_info_bsid =
{
  {
    -1,
    (gchar *) "Bsid",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "bsid",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _nmdbus_device_wi_max_property_info_active_nsp =
{
  {
    -1,
    (gchar *) "ActiveNsp",
    (gchar *) "o",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "active-nsp",
  FALSE,
  TRUE
};

static const GDBusPropertyInfo * const _nmdbus_device_wi_max_property_info_pointers[] =
{
  &_nmdbus_device_wi_max_property_info_nsps.parent_struct,
  &_nmdbus_device_wi_max_property_info_hw_address.parent_struct,
  &_nmdbus_device_wi_max_property_info_center_frequency.parent_struct,
  &_nmdbus_device_wi_max_property_info_rssi.parent_struct,
  &_nmdbus_device_wi_max_property_info_cinr.parent_struct,
  &_nmdbus_device_wi_max_property_info_tx_power.parent_struct,
  &_nmdbus_device_wi_max_property_info_bsid.parent_struct,
  &_nmdbus_device_wi_max_property_info_active_nsp.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _nmdbus_device_wi_max_interface_info =
{
  {
    -1,
    (gchar *) "org.freedesktop.NetworkManager.Device.WiMax",
    (GDBusMethodInfo **) &_nmdbus_device_wi_max_method_info_pointers,
    (GDBusSignalInfo **) &_nmdbus_device_wi_max_signal_info_pointers,
    (GDBusPropertyInfo **) &_nmdbus_device_wi_max_property_info_pointers,
    NULL
  },
  "device-wi-max",
};


/**
 * nmdbus_device_wi_max_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-freedesktop-NetworkManager-Device-WiMax.top_of_page">org.freedesktop.NetworkManager.Device.WiMax</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
nmdbus_device_wi_max_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_nmdbus_device_wi_max_interface_info.parent_struct;
}

/**
 * nmdbus_device_wi_max_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #NMDBusDeviceWiMax interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
nmdbus_device_wi_max_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "nsps");
  g_object_class_override_property (klass, property_id_begin++, "hw-address");
  g_object_class_override_property (klass, property_id_begin++, "center-frequency");
  g_object_class_override_property (klass, property_id_begin++, "rssi");
  g_object_class_override_property (klass, property_id_begin++, "cinr");
  g_object_class_override_property (klass, property_id_begin++, "tx-power");
  g_object_class_override_property (klass, property_id_begin++, "bsid");
  g_object_class_override_property (klass, property_id_begin++, "active-nsp");
  return property_id_begin - 1;
}



/**
 * NMDBusDeviceWiMax:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-NetworkManager-Device-WiMax.top_of_page">org.freedesktop.NetworkManager.Device.WiMax</link>.
 */

/**
 * NMDBusDeviceWiMaxIface:
 * @parent_iface: The parent interface.
 * @handle_get_nsp_list: Handler for the #NMDBusDeviceWiMax::handle-get-nsp-list signal.
 * @get_active_nsp: Getter for the #NMDBusDeviceWiMax:active-nsp property.
 * @get_bsid: Getter for the #NMDBusDeviceWiMax:bsid property.
 * @get_center_frequency: Getter for the #NMDBusDeviceWiMax:center-frequency property.
 * @get_cinr: Getter for the #NMDBusDeviceWiMax:cinr property.
 * @get_hw_address: Getter for the #NMDBusDeviceWiMax:hw-address property.
 * @get_nsps: Getter for the #NMDBusDeviceWiMax:nsps property.
 * @get_rssi: Getter for the #NMDBusDeviceWiMax:rssi property.
 * @get_tx_power: Getter for the #NMDBusDeviceWiMax:tx-power property.
 * @nsp_added: Handler for the #NMDBusDeviceWiMax::nsp-added signal.
 * @nsp_removed: Handler for the #NMDBusDeviceWiMax::nsp-removed signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-NetworkManager-Device-WiMax.top_of_page">org.freedesktop.NetworkManager.Device.WiMax</link>.
 */

typedef NMDBusDeviceWiMaxIface NMDBusDeviceWiMaxInterface;
G_DEFINE_INTERFACE (NMDBusDeviceWiMax, nmdbus_device_wi_max, G_TYPE_OBJECT)

static void
nmdbus_device_wi_max_default_init (NMDBusDeviceWiMaxIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * NMDBusDeviceWiMax::handle-get-nsp-list:
   * @object: A #NMDBusDeviceWiMax.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-NetworkManager-Device-WiMax.GetNspList">GetNspList()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call nmdbus_device_wi_max_complete_get_nsp_list() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-nsp-list",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NMDBusDeviceWiMaxIface, handle_get_nsp_list),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /* GObject signals for received D-Bus signals: */
  /**
   * NMDBusDeviceWiMax::nsp-added:
   * @object: A #NMDBusDeviceWiMax.
   * @arg_nsp: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-freedesktop-NetworkManager-Device-WiMax.NspAdded">"NspAdded"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("nsp-added",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NMDBusDeviceWiMaxIface, nsp_added),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_STRING);

  /**
   * NMDBusDeviceWiMax::nsp-removed:
   * @object: A #NMDBusDeviceWiMax.
   * @arg_nsp: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-freedesktop-NetworkManager-Device-WiMax.NspRemoved">"NspRemoved"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("nsp-removed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (NMDBusDeviceWiMaxIface, nsp_removed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_STRING);

  /* GObject properties for D-Bus properties: */
  /**
   * NMDBusDeviceWiMax:nsps:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.Nsps">"Nsps"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("nsps", "Nsps", "Nsps", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NMDBusDeviceWiMax:hw-address:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.HwAddress">"HwAddress"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("hw-address", "HwAddress", "HwAddress", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NMDBusDeviceWiMax:center-frequency:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.CenterFrequency">"CenterFrequency"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("center-frequency", "CenterFrequency", "CenterFrequency", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NMDBusDeviceWiMax:rssi:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.Rssi">"Rssi"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_int ("rssi", "Rssi", "Rssi", G_MININT32, G_MAXINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NMDBusDeviceWiMax:cinr:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.Cinr">"Cinr"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_int ("cinr", "Cinr", "Cinr", G_MININT32, G_MAXINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NMDBusDeviceWiMax:tx-power:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.TxPower">"TxPower"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_int ("tx-power", "TxPower", "TxPower", G_MININT32, G_MAXINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NMDBusDeviceWiMax:bsid:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.Bsid">"Bsid"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("bsid", "Bsid", "Bsid", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * NMDBusDeviceWiMax:active-nsp:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.ActiveNsp">"ActiveNsp"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("active-nsp", "ActiveNsp", "ActiveNsp", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * nmdbus_device_wi_max_get_nsps: (skip)
 * @object: A #NMDBusDeviceWiMax.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.Nsps">"Nsps"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nmdbus_device_wi_max_dup_nsps() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
nmdbus_device_wi_max_get_nsps (NMDBusDeviceWiMax *object)
{
  return NMDBUS_DEVICE_WI_MAX_GET_IFACE (object)->get_nsps (object);
}

/**
 * nmdbus_device_wi_max_dup_nsps: (skip)
 * @object: A #NMDBusDeviceWiMax.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.Nsps">"Nsps"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
nmdbus_device_wi_max_dup_nsps (NMDBusDeviceWiMax *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "nsps", &value, NULL);
  return value;
}

/**
 * nmdbus_device_wi_max_set_nsps: (skip)
 * @object: A #NMDBusDeviceWiMax.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.Nsps">"Nsps"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_device_wi_max_set_nsps (NMDBusDeviceWiMax *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "nsps", value, NULL);
}

/**
 * nmdbus_device_wi_max_get_hw_address: (skip)
 * @object: A #NMDBusDeviceWiMax.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.HwAddress">"HwAddress"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nmdbus_device_wi_max_dup_hw_address() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
nmdbus_device_wi_max_get_hw_address (NMDBusDeviceWiMax *object)
{
  return NMDBUS_DEVICE_WI_MAX_GET_IFACE (object)->get_hw_address (object);
}

/**
 * nmdbus_device_wi_max_dup_hw_address: (skip)
 * @object: A #NMDBusDeviceWiMax.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.HwAddress">"HwAddress"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
nmdbus_device_wi_max_dup_hw_address (NMDBusDeviceWiMax *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "hw-address", &value, NULL);
  return value;
}

/**
 * nmdbus_device_wi_max_set_hw_address: (skip)
 * @object: A #NMDBusDeviceWiMax.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.HwAddress">"HwAddress"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_device_wi_max_set_hw_address (NMDBusDeviceWiMax *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "hw-address", value, NULL);
}

/**
 * nmdbus_device_wi_max_get_center_frequency: (skip)
 * @object: A #NMDBusDeviceWiMax.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.CenterFrequency">"CenterFrequency"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
nmdbus_device_wi_max_get_center_frequency (NMDBusDeviceWiMax *object)
{
  return NMDBUS_DEVICE_WI_MAX_GET_IFACE (object)->get_center_frequency (object);
}

/**
 * nmdbus_device_wi_max_set_center_frequency: (skip)
 * @object: A #NMDBusDeviceWiMax.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.CenterFrequency">"CenterFrequency"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_device_wi_max_set_center_frequency (NMDBusDeviceWiMax *object, guint value)
{
  g_object_set (G_OBJECT (object), "center-frequency", value, NULL);
}

/**
 * nmdbus_device_wi_max_get_rssi: (skip)
 * @object: A #NMDBusDeviceWiMax.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.Rssi">"Rssi"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gint 
nmdbus_device_wi_max_get_rssi (NMDBusDeviceWiMax *object)
{
  return NMDBUS_DEVICE_WI_MAX_GET_IFACE (object)->get_rssi (object);
}

/**
 * nmdbus_device_wi_max_set_rssi: (skip)
 * @object: A #NMDBusDeviceWiMax.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.Rssi">"Rssi"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_device_wi_max_set_rssi (NMDBusDeviceWiMax *object, gint value)
{
  g_object_set (G_OBJECT (object), "rssi", value, NULL);
}

/**
 * nmdbus_device_wi_max_get_cinr: (skip)
 * @object: A #NMDBusDeviceWiMax.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.Cinr">"Cinr"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gint 
nmdbus_device_wi_max_get_cinr (NMDBusDeviceWiMax *object)
{
  return NMDBUS_DEVICE_WI_MAX_GET_IFACE (object)->get_cinr (object);
}

/**
 * nmdbus_device_wi_max_set_cinr: (skip)
 * @object: A #NMDBusDeviceWiMax.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.Cinr">"Cinr"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_device_wi_max_set_cinr (NMDBusDeviceWiMax *object, gint value)
{
  g_object_set (G_OBJECT (object), "cinr", value, NULL);
}

/**
 * nmdbus_device_wi_max_get_tx_power: (skip)
 * @object: A #NMDBusDeviceWiMax.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.TxPower">"TxPower"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gint 
nmdbus_device_wi_max_get_tx_power (NMDBusDeviceWiMax *object)
{
  return NMDBUS_DEVICE_WI_MAX_GET_IFACE (object)->get_tx_power (object);
}

/**
 * nmdbus_device_wi_max_set_tx_power: (skip)
 * @object: A #NMDBusDeviceWiMax.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.TxPower">"TxPower"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_device_wi_max_set_tx_power (NMDBusDeviceWiMax *object, gint value)
{
  g_object_set (G_OBJECT (object), "tx-power", value, NULL);
}

/**
 * nmdbus_device_wi_max_get_bsid: (skip)
 * @object: A #NMDBusDeviceWiMax.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.Bsid">"Bsid"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nmdbus_device_wi_max_dup_bsid() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
nmdbus_device_wi_max_get_bsid (NMDBusDeviceWiMax *object)
{
  return NMDBUS_DEVICE_WI_MAX_GET_IFACE (object)->get_bsid (object);
}

/**
 * nmdbus_device_wi_max_dup_bsid: (skip)
 * @object: A #NMDBusDeviceWiMax.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.Bsid">"Bsid"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
nmdbus_device_wi_max_dup_bsid (NMDBusDeviceWiMax *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "bsid", &value, NULL);
  return value;
}

/**
 * nmdbus_device_wi_max_set_bsid: (skip)
 * @object: A #NMDBusDeviceWiMax.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.Bsid">"Bsid"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_device_wi_max_set_bsid (NMDBusDeviceWiMax *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "bsid", value, NULL);
}

/**
 * nmdbus_device_wi_max_get_active_nsp: (skip)
 * @object: A #NMDBusDeviceWiMax.
 *
 * Gets the value of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.ActiveNsp">"ActiveNsp"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use nmdbus_device_wi_max_dup_active_nsp() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
nmdbus_device_wi_max_get_active_nsp (NMDBusDeviceWiMax *object)
{
  return NMDBUS_DEVICE_WI_MAX_GET_IFACE (object)->get_active_nsp (object);
}

/**
 * nmdbus_device_wi_max_dup_active_nsp: (skip)
 * @object: A #NMDBusDeviceWiMax.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.ActiveNsp">"ActiveNsp"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
nmdbus_device_wi_max_dup_active_nsp (NMDBusDeviceWiMax *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "active-nsp", &value, NULL);
  return value;
}

/**
 * nmdbus_device_wi_max_set_active_nsp: (skip)
 * @object: A #NMDBusDeviceWiMax.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-freedesktop-NetworkManager-Device-WiMax.ActiveNsp">"ActiveNsp"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
nmdbus_device_wi_max_set_active_nsp (NMDBusDeviceWiMax *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "active-nsp", value, NULL);
}

/**
 * nmdbus_device_wi_max_emit_nsp_added:
 * @object: A #NMDBusDeviceWiMax.
 * @arg_nsp: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-freedesktop-NetworkManager-Device-WiMax.NspAdded">"NspAdded"</link> D-Bus signal.
 */
void
nmdbus_device_wi_max_emit_nsp_added (
    NMDBusDeviceWiMax *object,
    const gchar *arg_nsp)
{
  g_signal_emit_by_name (object, "nsp-added", arg_nsp);
}

/**
 * nmdbus_device_wi_max_emit_nsp_removed:
 * @object: A #NMDBusDeviceWiMax.
 * @arg_nsp: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-freedesktop-NetworkManager-Device-WiMax.NspRemoved">"NspRemoved"</link> D-Bus signal.
 */
void
nmdbus_device_wi_max_emit_nsp_removed (
    NMDBusDeviceWiMax *object,
    const gchar *arg_nsp)
{
  g_signal_emit_by_name (object, "nsp-removed", arg_nsp);
}

/**
 * nmdbus_device_wi_max_call_get_nsp_list:
 * @proxy: A #NMDBusDeviceWiMaxProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-NetworkManager-Device-WiMax.GetNspList">GetNspList()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call nmdbus_device_wi_max_call_get_nsp_list_finish() to get the result of the operation.
 *
 * See nmdbus_device_wi_max_call_get_nsp_list_sync() for the synchronous, blocking version of this method.
 */
void
nmdbus_device_wi_max_call_get_nsp_list (
    NMDBusDeviceWiMax *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GetNspList",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * nmdbus_device_wi_max_call_get_nsp_list_finish:
 * @proxy: A #NMDBusDeviceWiMaxProxy.
 * @out_nsps: (out) (optional) (array zero-terminated=1): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nmdbus_device_wi_max_call_get_nsp_list().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with nmdbus_device_wi_max_call_get_nsp_list().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nmdbus_device_wi_max_call_get_nsp_list_finish (
    NMDBusDeviceWiMax *proxy,
    gchar ***out_nsps,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(^ao)",
                 out_nsps);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nmdbus_device_wi_max_call_get_nsp_list_sync:
 * @proxy: A #NMDBusDeviceWiMaxProxy.
 * @out_nsps: (out) (optional) (array zero-terminated=1): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-NetworkManager-Device-WiMax.GetNspList">GetNspList()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See nmdbus_device_wi_max_call_get_nsp_list() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
nmdbus_device_wi_max_call_get_nsp_list_sync (
    NMDBusDeviceWiMax *proxy,
    gchar ***out_nsps,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GetNspList",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(^ao)",
                 out_nsps);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * nmdbus_device_wi_max_complete_get_nsp_list:
 * @object: A #NMDBusDeviceWiMax.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @nsps: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-NetworkManager-Device-WiMax.GetNspList">GetNspList()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
nmdbus_device_wi_max_complete_get_nsp_list (
    NMDBusDeviceWiMax *object,
    GDBusMethodInvocation *invocation,
    const gchar *const *nsps)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(^ao)",
                   nsps));
}

/* ------------------------------------------------------------------------ */

/**
 * NMDBusDeviceWiMaxProxy:
 *
 * The #NMDBusDeviceWiMaxProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * NMDBusDeviceWiMaxProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #NMDBusDeviceWiMaxProxy.
 */

struct _NMDBusDeviceWiMaxProxyPrivate
{
  GData *qdata;
};

static void nmdbus_device_wi_max_proxy_iface_init (NMDBusDeviceWiMaxIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (NMDBusDeviceWiMaxProxy, nmdbus_device_wi_max_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (NMDBusDeviceWiMaxProxy)
                         G_IMPLEMENT_INTERFACE (NMDBUS_TYPE_DEVICE_WI_MAX, nmdbus_device_wi_max_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (NMDBusDeviceWiMaxProxy, nmdbus_device_wi_max_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (NMDBUS_TYPE_DEVICE_WI_MAX, nmdbus_device_wi_max_proxy_iface_init))

#endif
static void
nmdbus_device_wi_max_proxy_finalize (GObject *object)
{
  NMDBusDeviceWiMaxProxy *proxy = NMDBUS_DEVICE_WI_MAX_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (nmdbus_device_wi_max_proxy_parent_class)->finalize (object);
}

static void
nmdbus_device_wi_max_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 8);
  info = (const _ExtendedGDBusPropertyInfo *) _nmdbus_device_wi_max_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
nmdbus_device_wi_max_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface org.freedesktop.NetworkManager.Device.WiMax: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
nmdbus_device_wi_max_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 8);
  info = (const _ExtendedGDBusPropertyInfo *) _nmdbus_device_wi_max_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.freedesktop.NetworkManager.Device.WiMax", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) nmdbus_device_wi_max_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
nmdbus_device_wi_max_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_nmdbus_device_wi_max_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], NMDBUS_TYPE_DEVICE_WI_MAX);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, NMDBUS_TYPE_DEVICE_WI_MAX);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
nmdbus_device_wi_max_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  NMDBusDeviceWiMaxProxy *proxy = NMDBUS_DEVICE_WI_MAX_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nmdbus_device_wi_max_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nmdbus_device_wi_max_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *const *
nmdbus_device_wi_max_proxy_get_nsps (NMDBusDeviceWiMax *object)
{
  NMDBusDeviceWiMaxProxy *proxy = NMDBUS_DEVICE_WI_MAX_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  value = g_datalist_get_data (&proxy->priv->qdata, "Nsps");
  if (value != NULL)
    return value;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Nsps");
  if (variant != NULL)
    {
      value = g_variant_get_objv (variant, NULL);
      g_datalist_set_data_full (&proxy->priv->qdata, "Nsps", (gpointer) value, g_free);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
nmdbus_device_wi_max_proxy_get_hw_address (NMDBusDeviceWiMax *object)
{
  NMDBusDeviceWiMaxProxy *proxy = NMDBUS_DEVICE_WI_MAX_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "HwAddress");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
nmdbus_device_wi_max_proxy_get_center_frequency (NMDBusDeviceWiMax *object)
{
  NMDBusDeviceWiMaxProxy *proxy = NMDBUS_DEVICE_WI_MAX_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "CenterFrequency");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gint 
nmdbus_device_wi_max_proxy_get_rssi (NMDBusDeviceWiMax *object)
{
  NMDBusDeviceWiMaxProxy *proxy = NMDBUS_DEVICE_WI_MAX_PROXY (object);
  GVariant *variant;
  gint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Rssi");
  if (variant != NULL)
    {
      value = g_variant_get_int32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gint 
nmdbus_device_wi_max_proxy_get_cinr (NMDBusDeviceWiMax *object)
{
  NMDBusDeviceWiMaxProxy *proxy = NMDBUS_DEVICE_WI_MAX_PROXY (object);
  GVariant *variant;
  gint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Cinr");
  if (variant != NULL)
    {
      value = g_variant_get_int32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gint 
nmdbus_device_wi_max_proxy_get_tx_power (NMDBusDeviceWiMax *object)
{
  NMDBusDeviceWiMaxProxy *proxy = NMDBUS_DEVICE_WI_MAX_PROXY (object);
  GVariant *variant;
  gint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "TxPower");
  if (variant != NULL)
    {
      value = g_variant_get_int32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
nmdbus_device_wi_max_proxy_get_bsid (NMDBusDeviceWiMax *object)
{
  NMDBusDeviceWiMaxProxy *proxy = NMDBUS_DEVICE_WI_MAX_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Bsid");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
nmdbus_device_wi_max_proxy_get_active_nsp (NMDBusDeviceWiMax *object)
{
  NMDBusDeviceWiMaxProxy *proxy = NMDBUS_DEVICE_WI_MAX_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "ActiveNsp");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static void
nmdbus_device_wi_max_proxy_init (NMDBusDeviceWiMaxProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = nmdbus_device_wi_max_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, NMDBUS_TYPE_DEVICE_WI_MAX_PROXY, NMDBusDeviceWiMaxProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), nmdbus_device_wi_max_interface_info ());
}

static void
nmdbus_device_wi_max_proxy_class_init (NMDBusDeviceWiMaxProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = nmdbus_device_wi_max_proxy_finalize;
  gobject_class->get_property = nmdbus_device_wi_max_proxy_get_property;
  gobject_class->set_property = nmdbus_device_wi_max_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = nmdbus_device_wi_max_proxy_g_signal;
  proxy_class->g_properties_changed = nmdbus_device_wi_max_proxy_g_properties_changed;

  nmdbus_device_wi_max_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (NMDBusDeviceWiMaxProxyPrivate));
#endif
}

static void
nmdbus_device_wi_max_proxy_iface_init (NMDBusDeviceWiMaxIface *iface)
{
  iface->get_nsps = nmdbus_device_wi_max_proxy_get_nsps;
  iface->get_hw_address = nmdbus_device_wi_max_proxy_get_hw_address;
  iface->get_center_frequency = nmdbus_device_wi_max_proxy_get_center_frequency;
  iface->get_rssi = nmdbus_device_wi_max_proxy_get_rssi;
  iface->get_cinr = nmdbus_device_wi_max_proxy_get_cinr;
  iface->get_tx_power = nmdbus_device_wi_max_proxy_get_tx_power;
  iface->get_bsid = nmdbus_device_wi_max_proxy_get_bsid;
  iface->get_active_nsp = nmdbus_device_wi_max_proxy_get_active_nsp;
}

/**
 * nmdbus_device_wi_max_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-NetworkManager-Device-WiMax.top_of_page">org.freedesktop.NetworkManager.Device.WiMax</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call nmdbus_device_wi_max_proxy_new_finish() to get the result of the operation.
 *
 * See nmdbus_device_wi_max_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
nmdbus_device_wi_max_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (NMDBUS_TYPE_DEVICE_WI_MAX_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.NetworkManager.Device.WiMax", NULL);
}

/**
 * nmdbus_device_wi_max_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nmdbus_device_wi_max_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with nmdbus_device_wi_max_proxy_new().
 *
 * Returns: (transfer full) (type NMDBusDeviceWiMaxProxy): The constructed proxy object or %NULL if @error is set.
 */
NMDBusDeviceWiMax *
nmdbus_device_wi_max_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return NMDBUS_DEVICE_WI_MAX (ret);
  else
    return NULL;
}

/**
 * nmdbus_device_wi_max_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-NetworkManager-Device-WiMax.top_of_page">org.freedesktop.NetworkManager.Device.WiMax</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See nmdbus_device_wi_max_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type NMDBusDeviceWiMaxProxy): The constructed proxy object or %NULL if @error is set.
 */
NMDBusDeviceWiMax *
nmdbus_device_wi_max_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (NMDBUS_TYPE_DEVICE_WI_MAX_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.NetworkManager.Device.WiMax", NULL);
  if (ret != NULL)
    return NMDBUS_DEVICE_WI_MAX (ret);
  else
    return NULL;
}


/**
 * nmdbus_device_wi_max_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like nmdbus_device_wi_max_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call nmdbus_device_wi_max_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See nmdbus_device_wi_max_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
nmdbus_device_wi_max_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (NMDBUS_TYPE_DEVICE_WI_MAX_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.NetworkManager.Device.WiMax", NULL);
}

/**
 * nmdbus_device_wi_max_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to nmdbus_device_wi_max_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with nmdbus_device_wi_max_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type NMDBusDeviceWiMaxProxy): The constructed proxy object or %NULL if @error is set.
 */
NMDBusDeviceWiMax *
nmdbus_device_wi_max_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return NMDBUS_DEVICE_WI_MAX (ret);
  else
    return NULL;
}

/**
 * nmdbus_device_wi_max_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like nmdbus_device_wi_max_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See nmdbus_device_wi_max_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type NMDBusDeviceWiMaxProxy): The constructed proxy object or %NULL if @error is set.
 */
NMDBusDeviceWiMax *
nmdbus_device_wi_max_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (NMDBUS_TYPE_DEVICE_WI_MAX_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.NetworkManager.Device.WiMax", NULL);
  if (ret != NULL)
    return NMDBUS_DEVICE_WI_MAX (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * NMDBusDeviceWiMaxSkeleton:
 *
 * The #NMDBusDeviceWiMaxSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * NMDBusDeviceWiMaxSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #NMDBusDeviceWiMaxSkeleton.
 */

struct _NMDBusDeviceWiMaxSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_nmdbus_device_wi_max_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  NMDBusDeviceWiMaxSkeleton *skeleton = NMDBUS_DEVICE_WI_MAX_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], NMDBUS_TYPE_DEVICE_WI_MAX);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, NMDBUS_TYPE_DEVICE_WI_MAX);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_nmdbus_device_wi_max_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  NMDBusDeviceWiMaxSkeleton *skeleton = NMDBUS_DEVICE_WI_MAX_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nmdbus_device_wi_max_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_nmdbus_device_wi_max_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  NMDBusDeviceWiMaxSkeleton *skeleton = NMDBUS_DEVICE_WI_MAX_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_nmdbus_device_wi_max_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _nmdbus_device_wi_max_skeleton_vtable =
{
  _nmdbus_device_wi_max_skeleton_handle_method_call,
  _nmdbus_device_wi_max_skeleton_handle_get_property,
  _nmdbus_device_wi_max_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
nmdbus_device_wi_max_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return nmdbus_device_wi_max_interface_info ();
}

static GDBusInterfaceVTable *
nmdbus_device_wi_max_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_nmdbus_device_wi_max_skeleton_vtable;
}

static GVariant *
nmdbus_device_wi_max_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  NMDBusDeviceWiMaxSkeleton *skeleton = NMDBUS_DEVICE_WI_MAX_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_nmdbus_device_wi_max_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _nmdbus_device_wi_max_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _nmdbus_device_wi_max_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _nmdbus_device_wi_max_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.NetworkManager.Device.WiMax", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _nmdbus_device_wi_max_emit_changed (gpointer user_data);

static void
nmdbus_device_wi_max_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  NMDBusDeviceWiMaxSkeleton *skeleton = NMDBUS_DEVICE_WI_MAX_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _nmdbus_device_wi_max_emit_changed (skeleton);
}

static void
_nmdbus_device_wi_max_on_signal_nsp_added (
    NMDBusDeviceWiMax *object,
    const gchar *arg_nsp)
{
  NMDBusDeviceWiMaxSkeleton *skeleton = NMDBUS_DEVICE_WI_MAX_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(o)",
                   arg_nsp));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.NetworkManager.Device.WiMax", "NspAdded",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_nmdbus_device_wi_max_on_signal_nsp_removed (
    NMDBusDeviceWiMax *object,
    const gchar *arg_nsp)
{
  NMDBusDeviceWiMaxSkeleton *skeleton = NMDBUS_DEVICE_WI_MAX_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(o)",
                   arg_nsp));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.NetworkManager.Device.WiMax", "NspRemoved",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void nmdbus_device_wi_max_skeleton_iface_init (NMDBusDeviceWiMaxIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (NMDBusDeviceWiMaxSkeleton, nmdbus_device_wi_max_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (NMDBusDeviceWiMaxSkeleton)
                         G_IMPLEMENT_INTERFACE (NMDBUS_TYPE_DEVICE_WI_MAX, nmdbus_device_wi_max_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (NMDBusDeviceWiMaxSkeleton, nmdbus_device_wi_max_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (NMDBUS_TYPE_DEVICE_WI_MAX, nmdbus_device_wi_max_skeleton_iface_init))

#endif
static void
nmdbus_device_wi_max_skeleton_finalize (GObject *object)
{
  NMDBusDeviceWiMaxSkeleton *skeleton = NMDBUS_DEVICE_WI_MAX_SKELETON (object);
  guint n;
  for (n = 0; n < 8; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (nmdbus_device_wi_max_skeleton_parent_class)->finalize (object);
}

static void
nmdbus_device_wi_max_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  NMDBusDeviceWiMaxSkeleton *skeleton = NMDBUS_DEVICE_WI_MAX_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 8);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_nmdbus_device_wi_max_emit_changed (gpointer user_data)
{
  NMDBusDeviceWiMaxSkeleton *skeleton = NMDBUS_DEVICE_WI_MAX_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.freedesktop.NetworkManager.Device.WiMax",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_nmdbus_device_wi_max_schedule_emit_changed (NMDBusDeviceWiMaxSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
nmdbus_device_wi_max_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  NMDBusDeviceWiMaxSkeleton *skeleton = NMDBUS_DEVICE_WI_MAX_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _nmdbus_device_wi_max_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _nmdbus_device_wi_max_emit_changed");
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
nmdbus_device_wi_max_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  NMDBusDeviceWiMaxSkeleton *skeleton = NMDBUS_DEVICE_WI_MAX_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 8);
  info = (const _ExtendedGDBusPropertyInfo *) _nmdbus_device_wi_max_property_info_pointers[prop_id - 1];
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL &&
          info->emits_changed_signal)
        _nmdbus_device_wi_max_schedule_emit_changed (skeleton, info, prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
nmdbus_device_wi_max_skeleton_init (NMDBusDeviceWiMaxSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = nmdbus_device_wi_max_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, NMDBUS_TYPE_DEVICE_WI_MAX_SKELETON, NMDBusDeviceWiMaxSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 8);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRV);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[3], G_TYPE_INT);
  g_value_init (&skeleton->priv->properties[4], G_TYPE_INT);
  g_value_init (&skeleton->priv->properties[5], G_TYPE_INT);
  g_value_init (&skeleton->priv->properties[6], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[7], G_TYPE_STRING);
}

static const gchar *const *
nmdbus_device_wi_max_skeleton_get_nsps (NMDBusDeviceWiMax *object)
{
  NMDBusDeviceWiMaxSkeleton *skeleton = NMDBUS_DEVICE_WI_MAX_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
nmdbus_device_wi_max_skeleton_get_hw_address (NMDBusDeviceWiMax *object)
{
  NMDBusDeviceWiMaxSkeleton *skeleton = NMDBUS_DEVICE_WI_MAX_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
nmdbus_device_wi_max_skeleton_get_center_frequency (NMDBusDeviceWiMax *object)
{
  NMDBusDeviceWiMaxSkeleton *skeleton = NMDBUS_DEVICE_WI_MAX_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gint 
nmdbus_device_wi_max_skeleton_get_rssi (NMDBusDeviceWiMax *object)
{
  NMDBusDeviceWiMaxSkeleton *skeleton = NMDBUS_DEVICE_WI_MAX_SKELETON (object);
  gint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_int (&(skeleton->priv->properties[3]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gint 
nmdbus_device_wi_max_skeleton_get_cinr (NMDBusDeviceWiMax *object)
{
  NMDBusDeviceWiMaxSkeleton *skeleton = NMDBUS_DEVICE_WI_MAX_SKELETON (object);
  gint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_int (&(skeleton->priv->properties[4]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gint 
nmdbus_device_wi_max_skeleton_get_tx_power (NMDBusDeviceWiMax *object)
{
  NMDBusDeviceWiMaxSkeleton *skeleton = NMDBUS_DEVICE_WI_MAX_SKELETON (object);
  gint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_int (&(skeleton->priv->properties[5]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
nmdbus_device_wi_max_skeleton_get_bsid (NMDBusDeviceWiMax *object)
{
  NMDBusDeviceWiMaxSkeleton *skeleton = NMDBUS_DEVICE_WI_MAX_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[6]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
nmdbus_device_wi_max_skeleton_get_active_nsp (NMDBusDeviceWiMax *object)
{
  NMDBusDeviceWiMaxSkeleton *skeleton = NMDBUS_DEVICE_WI_MAX_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[7]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
nmdbus_device_wi_max_skeleton_class_init (NMDBusDeviceWiMaxSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = nmdbus_device_wi_max_skeleton_finalize;
  gobject_class->get_property = nmdbus_device_wi_max_skeleton_get_property;
  gobject_class->set_property = nmdbus_device_wi_max_skeleton_set_property;
  gobject_class->notify       = nmdbus_device_wi_max_skeleton_notify;


  nmdbus_device_wi_max_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = nmdbus_device_wi_max_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = nmdbus_device_wi_max_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = nmdbus_device_wi_max_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = nmdbus_device_wi_max_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (NMDBusDeviceWiMaxSkeletonPrivate));
#endif
}

static void
nmdbus_device_wi_max_skeleton_iface_init (NMDBusDeviceWiMaxIface *iface)
{
  iface->nsp_added = _nmdbus_device_wi_max_on_signal_nsp_added;
  iface->nsp_removed = _nmdbus_device_wi_max_on_signal_nsp_removed;
  iface->get_nsps = nmdbus_device_wi_max_skeleton_get_nsps;
  iface->get_hw_address = nmdbus_device_wi_max_skeleton_get_hw_address;
  iface->get_center_frequency = nmdbus_device_wi_max_skeleton_get_center_frequency;
  iface->get_rssi = nmdbus_device_wi_max_skeleton_get_rssi;
  iface->get_cinr = nmdbus_device_wi_max_skeleton_get_cinr;
  iface->get_tx_power = nmdbus_device_wi_max_skeleton_get_tx_power;
  iface->get_bsid = nmdbus_device_wi_max_skeleton_get_bsid;
  iface->get_active_nsp = nmdbus_device_wi_max_skeleton_get_active_nsp;
}

/**
 * nmdbus_device_wi_max_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-NetworkManager-Device-WiMax.top_of_page">org.freedesktop.NetworkManager.Device.WiMax</link>.
 *
 * Returns: (transfer full) (type NMDBusDeviceWiMaxSkeleton): The skeleton object.
 */
NMDBusDeviceWiMax *
nmdbus_device_wi_max_skeleton_new (void)
{
  return NMDBUS_DEVICE_WI_MAX (g_object_new (NMDBUS_TYPE_DEVICE_WI_MAX_SKELETON, NULL));
}

